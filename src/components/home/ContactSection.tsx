'use client';

import React, { useState } from 'react';
import { motion } from 'motion/react';
import { SOCIALS } from '@/src/data/socials';
import { Mail, Send, Check, Copy, MessageSquare, Flame, Github } from 'lucide-react';

export default function ContactSection() {
  const [copied, setCopied] = useState(false), [sent, setSent] = useState(false), [sending, setSending] = useState(false), [error, setError] = useState<string | null>(null);
  const [formState, setFormState] = useState({ name: '', email: '', message: '' });
  const email = 'admin@aslynx.store';
  const handleCopy = () => { navigator.clipboard.writeText(email); setCopied(true); setTimeout(() => setCopied(false), 2000); };
  const handleSubmit = async (e: React.FormEvent) => { e.preventDefault(); if (!formState.name || !formState.email || !formState.message || sending) return; setSending(true); setError(null); try { const res = await fetch('/api/contact', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(formState) }); const data = await res.json(); if (!res.ok) throw new Error(data.error || 'Failed to send your message.'); setSent(true); } catch (err) { setError(err instanceof Error ? err.message : 'Failed to send your message. Please retry.'); } finally { setSending(false); } };
  const getSocialIcon = (name: string) => name === 'GitHub' ? <Github className="w-5 h-5" /> : name === 'CurseForge' ? <Flame className="w-5 h-5" /> : name === 'Discord' ? <MessageSquare className="w-5 h-5" /> : <Mail className="w-5 h-5" />;
  return <section id="contact" className="py-16 sm:py-20 px-4 sm:px-6 max-w-6xl mx-auto scroll-mt-24"><div className="text-center max-w-2xl mx-auto mb-12"><div className="inline-flex items-center gap-1.5 px-3 py-1 rounded-lg bg-cyan-500/10 border border-cyan-500/20 text-cyan-400 text-xs font-semibold uppercase tracking-wider mb-3"><Mail className="w-3.5 h-3.5" />Get in Touch</div><h2 className="text-3xl sm:text-4xl font-extrabold text-white tracking-tight mb-3">Let’s Build Something Exceptional</h2><p className="text-sm text-neutral-400">Discuss an AI project, custom Minecraft add-on, or modern web platform.</p></div><div className="grid grid-cols-1 md:grid-cols-2 gap-8 items-start"><motion.div initial={{ opacity: 0, x: -20 }} whileInView={{ opacity: 1, x: 0 }} viewport={{ once: true }} transition={{ duration: 0.5 }} className="space-y-6"><div className="bg-[#0f1219] p-6 sm:p-8 rounded-3xl border border-white/10 space-y-4"><h3 className="text-xl font-bold text-white">Direct Communication</h3><p className="text-xs sm:text-sm text-neutral-400 leading-relaxed">Send a direct message or connect across the platforms below.</p><div className="p-4 rounded-2xl bg-white/5 border border-white/10 flex items-center justify-between gap-3"><div className="flex items-center gap-3 overflow-hidden"><div className="w-8 h-8 rounded-lg bg-white/[0.07] border border-white/10 text-neutral-200 flex items-center justify-center shrink-0"><Mail className="w-4 h-4" /></div><span className="text-xs sm:text-sm text-neutral-200 font-medium truncate">{email}</span></div><button onClick={handleCopy} className="px-3 py-1.5 rounded-xl bg-white/10 hover:bg-white/20 text-xs font-medium text-white transition-all flex items-center gap-1.5 shrink-0">{copied ? <Check className="w-3.5 h-3.5" /> : <Copy className="w-3.5 h-3.5" />}<span>{copied ? 'Copied' : 'Copy'}</span></button></div></div><div className="grid grid-cols-2 gap-3">{SOCIALS.map((s) => <a key={s.name} href={s.url} target="_blank" rel="noreferrer" className="bg-[#0f1219] hover:bg-white/[0.03] p-4 rounded-2xl group transition-colors"><div className="mb-2 group-hover:scale-110 transition-transform">{getSocialIcon(s.name)}</div><h4 className="text-sm font-semibold text-white group-hover:text-neutral-200 transition-colors">{s.name}</h4><p className="text-[11px] text-neutral-400 truncate">{s.username}</p></a>)}</div></motion.div><motion.div initial={{ opacity: 0, x: 20 }} whileInView={{ opacity: 1, x: 0 }} viewport={{ once: true }} transition={{ duration: 0.5 }} className="bg-[#0f1219] p-6 sm:p-8 rounded-3xl border border-white/10"><h3 className="text-xl font-bold text-white mb-2">Send a Message</h3><p className="text-xs text-neutral-400 mb-6">Leave a note and your contact details.</p>{sent ? <div className="p-6 rounded-2xl bg-white/[0.05] border border-white/10 text-center space-y-2"><Check className="w-8 h-8 text-neutral-200 mx-auto" /><h4 className="text-base font-bold text-white">Message Received</h4><p className="text-xs text-neutral-400">I will respond to {formState.email} soon.</p></div> : <form onSubmit={handleSubmit} className="space-y-4">{error && <div role="alert" className="px-3 py-2 rounded-xl border border-white/10 bg-white/[0.04] text-xs text-neutral-300">{error}</div>}<div><label htmlFor="contact-name" className="block text-xs font-medium text-neutral-300 mb-1.5">Your Name</label><input id="contact-name" type="text" required autoComplete="name" placeholder="e.g. Alex Rivers" value={formState.name} onChange={(e) => setFormState({ ...formState, name: e.target.value })} className="w-full px-4 py-2.5 rounded-xl bg-black/40 border border-white/10 text-white text-xs sm:text-sm placeholder:text-neutral-600 focus:outline-none focus:border-white/30 transition-colors" /></div><div><label htmlFor="contact-email" className="block text-xs font-medium text-neutral-300 mb-1.5">Email Address</label><input id="contact-email" type="email" required autoComplete="email" placeholder="alex@example.com" value={formState.email} onChange={(e) => setFormState({ ...formState, email: e.target.value })} className="w-full px-4 py-2.5 rounded-xl bg-black/40 border border-white/10 text-white text-xs sm:text-sm placeholder:text-neutral-600 focus:outline-none focus:border-white/30 transition-colors" /></div><div><label htmlFor="contact-message" className="block text-xs font-medium text-neutral-300 mb-1.5">Message / Inquiry</label><textarea id="contact-message" rows={4} required maxLength={4000} placeholder="Tell me about your project, idea, or questions..." value={formState.message} onChange={(e) => setFormState({ ...formState, message: e.target.value })} className="w-full px-4 py-2.5 rounded-xl bg-black/40 border border-white/10 text-white text-xs sm:text-sm placeholder:text-neutral-600 focus:outline-none focus:border-white/30 transition-colors resize-none" /></div><button type="submit" disabled={sending} className="w-full inline-flex items-center justify-center gap-2 py-3 rounded-xl bg-cyan-400 hover:bg-cyan-300 text-black disabled:opacity-50 disabled:pointer-events-none font-semibold text-xs sm:text-sm transition-all active:scale-95"><Send className="w-4 h-4" /><span>{sending ? 'Sending…' : 'Send Message'}</span></button></form>}</motion.div></div></section>;
}
